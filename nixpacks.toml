# Nixpacks configuration for Railway
# This ensures FFmpeg is installed

[phases.setup]
nixPkgs = ["python310", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT:-8080} --workers 1 --timeout 300"
